Implement a non-post category , which is a small post which doesnt need a title or anything, just content
and tags. This would be akin to tweets. The intro page should show both posts and non-posts (we can call these
thoughts). We want both of them to show in overall chronological order, posts would have title along with the first 200 
characters whereas thoughts will have the complete content. We can have a separate folder for thoughts.

